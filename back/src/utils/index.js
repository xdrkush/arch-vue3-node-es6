import {
    createToken, verifyToken, regenerateToken, soonTokenExpired, timeTokenExpired
} from "./JsonWebToken"

module.exports = {
    // JsonWebToken
    createToken, verifyToken, regenerateToken, soonTokenExpired, timeTokenExpired
}